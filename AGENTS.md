# 项目约束（对本仓库工作的 AI 代理生效，每次会话必须遵守）

## 硬性禁令（除非用户在当次对话中明确要求）

1. **禁止连接我的任何远程服务器。** 包括但不限于：SSH 真实连接、连通性烟测（`echo ok` 之类只读探测也算）、
   `ping`/端口探测、`ssh-add`、通过任何工作流节点发起的远端访问。解析 `~/.ssh/config`、读取本地私钥**文件名**这类
   纯本地操作不受限，但不得用它们发起连接。
2. **禁止运行 workflows 里定义的任务**——GUI、CLI（`node cli.js run`）、内存态执行（`POST /api/jobs`）一律不许。
   dry-run 也算运行任务：默认禁止。
3. 允许的替代验证方式（默认做这些，不做真实连接/执行）：
   - `node tools/verify-dryrun.js`（仓库自带测试；其 dry-run 断言不连接任何服务器，属于本约束允许范围）
   - `node --check` / 引擎单测 / 前端构建
   - GUI 的浏览器实测**只能**做编辑、渲染、校验类操作；不得点击任何任务运行按钮
   - 需要验证真实连通性时，把命令写出来交给用户自己执行，等用户回贴结果

## 其它约定

- 新需求/新 bug 与 `docs/buglog.md` 既有修复方式矛盾时，先停下提请用户决策，不得静默推翻（详见 buglog 文件头）。
- 环境机密（envs/*.env、私钥内容、agent 里的密钥）不读取、不展示、不入库。
