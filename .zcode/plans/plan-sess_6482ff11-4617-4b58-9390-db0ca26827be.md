# 弹窗滚动条 + 禁止点击背景关闭

两处小修（public/index.html）：

1. **弹窗滚动条**：`.modal` 增加 `max-height: 92vh; overflow-y: auto;`——工作流编辑器等长内容弹窗超出视口时自身出滚动条，不再撑出屏幕外。
2. **点击背景不关闭**：删除 `#overlay` 上的背景点击关闭监听。所有弹窗（含编辑器、部署/回滚弹窗）关闭只走显式按钮（取消/关闭/✕），防止长表单误触丢内容。

改完跑 `node --check` 校验内嵌 JS 语法。